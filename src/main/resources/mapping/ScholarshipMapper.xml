<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.ScholarshipMapper">

    <insert id="addScholarship" parameterType="com.example.demo.entity.Scholarship" useGeneratedKeys="true"
            keyProperty="id" keyColumn="id">
        insert into scholarship values (null, #{name}, #{money}, #{count}, #{classCount}, #{description}, 1, now(), now())
    </insert>

    <update id="deleteScholarship" parameterType="integer">
        update scholarship set flag = 0 ,updateTime = now() where id = #{id}
    </update>

    <select id="getAllScholarship" resultType="com.example.demo.entity.Scholarship">
        select id, name, count, classCount, money, description, flag from scholarship where flag = 1
        limit #{nowPage}, #{pageSize}
    </select>

    <select id="getCount" resultType="integer">
        select count(*) from scholarship where flag = 1
    </select>

    <select id="getScholarshipbyId" resultType="com.example.demo.entity.Scholarship">
        select id, name, count, classCount, money, description, flag from scholarship where id = #{id} and flag = 1
    </select>

    <update id="updateScholarshipById" parameterType="com.example.demo.vo.request.ScholarshipVORequest">
        update scholarship
        set
        name = #{name},
        money = #{money},
        count = #{count},
        classCount = #{classCount},
        description = #{description},
        updateTime = now()
        where id = #{id}
    </update>
</mapper>

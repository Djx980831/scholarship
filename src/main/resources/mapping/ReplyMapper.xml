<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo.mapper.ReplyMapper">

    <insert id="addReplyByCommentId" parameterType="com.example.demo.vo.request.ReplyVORequest" useGeneratedKeys="true" keyColumn="id" keyProperty="id">
        insert into reply values (null, #{commentId}, #{reply}, #{replyUserId}, #{replyUserName}, 1, now(), now())
    </insert>

    <update id="deleteReplyById" parameterType="com.example.demo.vo.request.ReplyVORequest">
        update reply set flag = 0, updateTime = now()
        where id = #{id}
    </update>

    <select id="getAllReply" resultType="com.example.demo.entity.Reply">
        select * from reply where flag = 1 and commentId = #{commentId}
        limit #{nowPage}, #{pageSize}
    </select>

    <select id="getCountByCommentId" resultType="integer">
        select count(*) from reply where flag = 1 and commentId = #{commentId}
    </select>

    <select id="getReplyByReplyId" resultType="com.example.demo.entity.Reply">
        select * from reply where flag = 1 and id = #{id}
    </select>
</mapper>
